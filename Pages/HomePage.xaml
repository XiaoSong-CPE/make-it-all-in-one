<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="make_it_all_in_one.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:make_it_all_in_one.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
         Background="Transparent">

    <Grid>
         <ScrollViewer>
            <StackPanel Spacing="20" Padding="20">
                <!-- Header -->
                <TextBlock Text="WinUI 3 Gallery" FontSize="36" FontWeight="Bold" />
                <TextBlock Text="Windows App SDK 1.6" FontSize="18" />

                <!-- Main Sections -->
                <Grid ColumnDefinitions="*,*,*,*">
                    <StackPanel Orientation="Vertical" Spacing="10">
                        <Button Content="Getting started" HorizontalAlignment="Stretch" />
                        <TextBlock Text="Get started with WinUI and explore detailed documentation." TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Spacing="10">
                        <Button Content="Design" HorizontalAlignment="Stretch" />
                        <TextBlock Text="Guidelines and toolkits for creating stunning WinUI experiences." TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Spacing="10">
                        <Button Content="WinUI on GitHub" HorizontalAlignment="Stretch" />
                        <TextBlock Text="Explore the WinUI source code and repository." TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Spacing="10">
                        <Button Content="Community Toolkit" HorizontalAlignment="Stretch" />
                        <TextBlock Text="An open-source collection of helper functions, controls, and app services." TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <!-- Recently Added Samples -->
                <TextBlock Text="Recently added samples" FontSize="24" FontWeight="Bold" />
                <StackPanel Spacing="10">
                    <Border Background="LightGray" Padding="10" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="AnnotatedScrollBar" FontWeight="Bold" />
                            <TextBlock Text="A control that extends a regular vertical scrollbar's functionality for an easy navigation through large collections." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                    <Border Background="LightGray" Padding="10" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="Capture Element / Camera Preview" FontWeight="Bold" />
                            <TextBlock Text="A sample for doing a camera preview." TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
